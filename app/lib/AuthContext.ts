/**
 * Re-export useAuthContext dari services/useStateAuth.
 * Semua state dan method auth (user, loading, signIn, signOut, dll) ada di sana.
 */
export { useAuthContext } from "@/services/useStateAuth";
